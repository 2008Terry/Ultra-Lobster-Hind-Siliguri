# 给朋友的安装说明

## 安装

1. 在 GitHub 仓库页面点击 **Code → Download ZIP**。
2. 解压 ZIP，找到同时包含 `manifest.json`、`theme.css` 和 `fonts/` 的主题文件夹。
3. 把这个文件夹复制到你的 Obsidian vault：

   ```text
   <你的 Vault>/.obsidian/themes/Ultra Lobster Hind Siliguri/
   ```

4. 打开 Obsidian，进入 **设置 → 外观 → 主题**。
5. 选择 **Ultra Lobster Hind Siliguri**。

如果 `.obsidian/themes/` 不存在，可以手动创建。不要把 ZIP 外层的总文件夹误复制进去；`manifest.json` 和 `theme.css` 必须直接位于主题文件夹中。

## 更新

1. 从 GitHub 下载新版 ZIP。
2. 关闭 Obsidian，或先切换到其他主题。
3. 用新版主题文件夹替换 vault 中的 `Ultra Lobster Hind Siliguri` 文件夹。
4. 不要覆盖 `.obsidian` 下的其他设置、插件或工作区文件。

## 字体说明

主题已经包含 Hind Siliguri 字体文件，通常不需要另外安装字体。字体许可证位于主题目录的 `fonts/OFL.txt`。

## 卸载

在 Obsidian 中切换到其他主题后，删除 vault 中的：

```text
<你的 Vault>/.obsidian/themes/Ultra Lobster Hind Siliguri/
```
